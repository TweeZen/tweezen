pipeline {
    agent any
    environment {
        GIT_EMAIL = 'no-reply@jenkins.com'
        GIT_USER = 'Jenkins'
    }
    tools {
        maven 'Maven 3.6.2'
        jdk 'jdk8'
    }
    stages {
        stage ('Checking Tools') {
            steps {
                sh '''
                    echo 'user in use:' $USER
                    java -version
                    mvn --version
                    docker version
                '''
            }
        }
        stage('Generate Build ID') {
            steps {
                dir("retwisj"){
                    script {
                        def pom = readMavenPom file: 'pom.xml'
                        env.POM_VERSION = pom.version
                        env.BUILD_ID = "${env.POM_VERSION}-${env.BUILD_NUMBER}"
                        echo "${env.BUILD_ID}"
                    }
                }
            }
        }
        stage('Build') {
            steps {
                dir("retwisj"){
                    sh 'mvn -Dmaven.test.failure.ignore clean package'
                }
            }
        }
        stage('Google Authentification') {
            steps {
                sh '''
                    gcloud auth activate-service-account 592775981948-compute@developer.gserviceaccount.com --key-file=/opt/bitnami/k8s-credentials.json
                    gcloud auth configure-docker --quiet
                    gcloud auth list
                '''
            }
        }
        stage('Docker') {
            steps {
                dir("retwisj"){
                    sh '''
                        docker build -t tweezen:$BUILD_ID .
                        docker tag tweezen:$BUILD_ID eu.gcr.io/robust-zenith-161810/tweezen:$BUILD_ID
                        docker push eu.gcr.io/robust-zenith-161810/tweezen:$BUILD_ID
                    '''
                }
            }
        }
        stage('Deployment Modification') {
            steps {
                sshagent (credentials: ['git-ssh']) {
                    sh'''
                        git config --global user.email ${env.GIT_EMAIL}
                        git config --global user.name ${env.GIT_USER}
                        sed -i "s/#{BUILD_ID}#/${BUILD_ID}/" template/deployment_app.yaml
                        git clone git@github.com:TweeZen/tweezen-deployment.git
                        cp template/deployment_app.yaml tweezen-deployment/deployment_app.yaml
                    '''
                }
            }
        }
        stage('Git') {
            steps {
                sshagent (credentials: ['git-ssh']) {
                    dir("tweezen-deployment"){
                        sh'''
                            git add deployment_app.yaml
                            git commit -m 'deployment version ${env.BUILD_ID}'
                            git push origin HEAD:master
                        '''
                    }
                }
            }
        }
        // stage('Results') {
        //     junit '**/target/surefire-reports/TEST-*.xml'
        //     archiveArtifacts 'target/*.jar'
        // }
    }
    post {
        always {
            slackSend color: 'good', message: "🕓 [<${env.JOB_URL}|${env.JOB_NAME}>-<${env.BUILD_URL}|#${env.BUILD_NUMBER}>] Build Started with id ${env.BUILD_ID}"
        }
        success {
            slackSend color: 'good', message: "🐋 [<${env.JOB_URL}|${env.JOB_NAME}>-<${env.BUILD_URL}|#${env.BUILD_NUMBER}>] Docker image successfully built and pushed on GCR!"
        }
        failure {
            slackSend color: '#ff0000', message: "❌ [<${env.JOB_URL}|${env.JOB_NAME}>-<${env.BUILD_URL}|#${env.BUILD_NUMBER}>] Build Failed! (<${env.BUILD_URL}/console|view logs>)"
        }
    }
}